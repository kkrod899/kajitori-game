name: Validate production-scope experiment v1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Validation temporarily manual while v2 files are assembled"
