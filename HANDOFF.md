# Handoff

最終更新: 2026-09-05 / Codex

## 現在地

| 項目 | 状態 |
|---|---|
| プロダクト | 家事取りゲーム v0.2 HTMLモック |
| 実装 | `kajitori_minimal_pictogram_compact.html` 1ファイル。Slice 01実装済み |
| v0.2の核 | 状態主役、余力0〜3件、利用者が選ぶ現在の一手、静的先読み、証拠イベント |
| 旧初期データ | 8カテゴリ・50タスクの保存データは保持。v0.2では全面移植せず限定テンプレートで核を検証 |
| 保存 | ブラウザlocalStorage。既存キー`kajitori_stable_mvp_v2`を維持し、`v02`名前空間を追加 |
| Git | Public repository `kkrod899/kajitori-game` / `main` |
| 共有思想 | `docs/PRODUCT_BRIEF.md` |
| 実装仕様 | `docs/V0_2_UX_SPEC.md` — Slice 01 implemented / post-review Ready |
| Slice定義 | `docs/V0_2_SLICE_01.md` |
| 二層コンセプト | 状態主役の判断層 + 見えない仕事の目撃者となる尊厳層 |
| R2事後レビュー | `reviews/2026-07-23-0003-claude-v0-2-r2.md` 完了。4 FindingsすべてAccepted・仕様反映済み |
| 実装後レビュー | `reviews/2026-08-30-0004-chatgpt-v0-2-slice-01-post.md` — **Ready / P0・P1なし** |
| Decision | D-020〜D-023をAcceptedで追加。D-019名称変更はProposedのまま |
| 現在のフェーズ | 機能追加より先に、主要利用者本人による7〜14日実生活テストへ進む |
| スマホURL化 | GitHub Pages公開済み。`https://kkrod899.github.io/kajitori-game/` から現行v0.2を起動できる |

## 2026-08-30に完了したこと

### 1. R2レビュー対応

`reviews/2026-07-23-0003-claude-v0-2-r2.md` の指摘を次の通り処理した。

| Finding | 状態 | 解決 |
|---|---|---|
| 状態の鮮度が未定義 | Accepted | `十分 / そろそろ / 切れた`は確認した当日だけ現在事実。翌日は値を隠し、最終確認日または未確認を表示 |
| `起きなかったこと`の証拠境界が曖昧 | Accepted | `この2週間: 在庫切れ0回`等の期間ゼロ集計を廃止。固定証拠ルールを満たす出来事だけ表示 |
| 今日3件満杯時の先読み追加が未定義 | Accepted | 4件目を作らず、既存1件との明示的な入替または追加中止 |
| `あとで`の遷移が未定義 | Accepted | 当日は`今日のあとで`へ退避し自動復帰なし。翌日は前日の状態を盲目的に繰り越さず条件再評価 |

MVP肥大対策として、担当領域の選択・認定と通知設定UIをLaterへ移した。

### 2. 新規Decision

| ID | 状態 | 判断 |
|---|---|---|
| D-020 | Accepted | 「起きなかったこと」は期間の0回集計でなく、証拠のある出来事ベースに限定 |
| D-021 | Accepted | 3段階状態は確認当日だけ現在事実として扱う |
| D-022 | Accepted | 今日のアクティブは最大3件。満杯時は入替/中止。`あとで`は同日自動復帰しない |
| D-023 | Accepted | 担当選択・認定、通知設定UIをLaterへ。担当タブは完結ループ閲覧のみ |

### 3. Slice 01実装

`kajitori_minimal_pictogram_compact.html` に以下を実装した。

| 領域 | 実装 |
|---|---|
| 初回設定 | 3ステップ。赤ちゃん時期、上の子/保育園等、生活の枠。通知設定・担当認定なし |
| 今日 | 守る日 / 回す日 / 先回り日、0〜3件、状態主役カード、現在の一手 |
| 操作 | `今やる / 完了 / あとで / 今日は見送る / もう済んでいた`、Undo |
| 0件 | `今日は増やさなくて大丈夫`を表示し、未完了警告・繰越警告なし |
| 状態 | `十分 / そろそろ / 切れた`のみ。当日鮮度を強制 |
| 1日1問 | `分かる / 見てくる`。未回答ペナルティなし。`分かる`2回連続で3日だけ質問候補から外す。全領域クールダウン中は質問を休む |
| 先読み | 最大3件。今日へ自動投入せず、利用者が選択。満杯時は明示入替 |
| 担当 | 完結ループ閲覧のみ。担当認定・採点なし |
| 成長 | 固定証拠ルールを満たす出来事、自分で気づけた記録。XP・連続日数・夫婦比較・称賛なし |
| 保存 | 旧rootデータを保持し`v02`を追加。v0.2初期化も旧データを削除しない |
| 保存失敗 | localStorage書込例外時に操作をロールバックし、画面へエラー表示 |

現在のアプリ実装commit: `297946bc265b5a58c256898bfcb75149458f8cb0`

## 検証結果

`reviews/2026-08-30-0004-chatgpt-v0-2-slice-01-post.md` に記録済み。

| 検証 | 結果 |
|---|---|
| JavaScript構文チェック | Pass |
| 初回設定完走 | Pass |
| 旧localStorage保持 + `v02`追加 | Pass |
| 回す日2件 / 先回り日3件 | Pass |
| 余力3→1で選択中の現在の一手を優先保持 | Pass |
| 3件満杯時の先読み追加 | Pass — 4件目を作らない |
| `あとで`後の再レンダー/タブ移動 | Pass — 自動復帰なし |
| 前日状態の失効 | Pass — 古い値を現在状態として表示しない |
| `そろそろ`→補充完了の証拠生成 | Pass |
| `切れた`→補充で「切らす前」証拠を作らない | Pass |
| 全質問領域クールダウン | Pass — 質問を強制しない |
| 保存失敗 | Pass — ロールバック + エラー表示 |
| 主要ボタン44px相当以上 | Pass |
| 4タブ、XP・達成率・期間0回表示なし | Pass |

事後レビュー総合判定は **Ready**。P0/P1なし。

## 既知問題・残存リスク

| Severity | 問題 | 状態 |
|---|---|---|
| P2 | 保育園の先読みは現状「次の平日」を登園候補として扱うため、祝日・個別休園・非登園日を知らない | Open。C-03の不要提案率として7〜14日テストで測る。必要なら後続で任意の「次の登園日」設定へ |
| Scope | 旧50タスクをすべてv0.2化していない | Intentional。Slice 01では核の検証を優先 |
| Scope | ゲーム成長表現は未完成 | Later。件数XPへ戻さず、実生活テスト後に能力/担当領域ベースで再設計 |

### 旧baseline問題の現在地

| 旧問題 | 現在地 |
|---|---|
| `明日リトライ`が表示だけ | v0.2では独立リトライを使わず、`あとで` + 翌日条件再評価へ統合。旧`retryQueue`データは保持するだけ |
| 条件一致タスクが42件・380分 | v0.2では今日のアクティブを余力に応じ0〜3件へ制限 |
| アプリ指示待ちになる | 状態事実を先に見せ、`今やる`は利用者が選択。1日1問で把握を育てる設計へ変更 |
| 新生児タスクの月齢依存 | Slice 01では月齢を断定に使わず確認のきっかけに限定。旧50タスク全体の発達対応は未移植 |
| Undo・保留等がない | Slice 01の中核操作では解消 |
| localStorage保存失敗が見えない | 解消。エラー表示 + ロールバック |
| 小さい操作が多い | Slice 01主要ボタンは44px相当以上へ |

## 次にやること

**次は機能追加ではなく、7〜14日の実生活テスト。** `docs/PRODUCT_BRIEF.md` のC-01〜C-05とD-014を検証する。

最低限、日々見るもの:

| 観点 | 見ること |
|---|---|
| C-01 余力 | 0〜3件でも圧迫感があるか / 着手しやすいか |
| C-02 理由 | 理由を読まなくても自分で判断できる項目が増えるか |
| C-03 先読み | 不要提案率、実際に必要になる前に準備できたか |
| C-04 状態主役 | 状態を見て自分で一手を選べたか、1日1問が鬱陶しくないか |
| C-05 証拠イベント | 表示された事実が本人の支えになるか、根拠が怪しい表示がないか |
| 自発性 | `提案になかったことに気づいた`が実際に増えるか |

初日から観察は残すが、数日だけの印象で機能を増減せず、原則7〜14日分を見てから次のSliceを決める。

### 次の実装候補は実利用後に判断

- 保育園の「次の登園日」静的設定
- テンプレートの追加/削除
- 状態質問の対象領域拡張
- 担当領域の選択・認定
- 能力/担当領域ベースのゲーム成長表現
- 家族共有 / 通知

現時点ではこれらを先に作らない。

## 再開時の読み順

1. `HANDOFF.md`
2. `docs/PRODUCT_BRIEF.md`
3. `docs/COLLABORATION_WORKFLOW.md`
4. `docs/DECISIONS.md`
5. `docs/V0_2_UX_SPEC.md`
6. `docs/V0_2_SLICE_01.md`
7. `reviews/2026-08-30-0004-chatgpt-v0-2-slice-01-post.md`

R2レビューは完了済みなので、次回はR2を再実行しない。実利用フィードバックがある場合は、その観察を正本として次Sliceの定義から再開する。

## 2026-09-05に完了したこと

### 3. スマホURL公開

公開前の現行ツリー・到達可能な履歴の監査で、固定パスワード・APIキー・秘密トークン・秘密鍵は確認されなかったため、ユーザーの許可に沿って公開した。

| 項目 | 確認結果 |
|---|---|
| リポジトリ | `kkrod899/kajitori-game` をPublicへ変更 |
| Pages公開元 | GitHub Actions |
| ワークフロー | [Publish the app to GitHub Pages #1](https://github.com/kkrod899/kajitori-game/actions/runs/33923047647) — Success |
| 公開URL | [https://kkrod899.github.io/kajitori-game/](https://kkrod899.github.io/kajitori-game/) |
| 表示確認 | 公開URLから`家事取りゲーム v0.2`を表示し、初回設定画面まで確認 |
| ホーム画面起動 | 公開manifestで`display: standalone`、起動先、180/192/512pxアイコンを確認 |
| 保存形式 | アプリ本体と`kajitori_stable_mvp_v2`は変更なし |

この公開方式では、リポジトリのソース・文書・レビュー・案件状態は誰でも閲覧できる。一方、利用データはサーバーへ送られず、URLを知るだけで他端末のlocalStorageを読める状態ではない。同じ端末・同じブラウザを使える人には保存データが見える点は残る。

## スマホURL化の現在地

| 項目 | 状態 |
|---|---|
| URL入口 | `index.html` が現行v0.2 HTMLを開く。GitHub PagesのプロジェクトURL直下を入口にできる |
| ホーム画面 | `manifest.webmanifest`、iPhone用メタデータ、180/192/512pxアイコンを追加 |
| オフライン補助 | 同じ端末で一度開いた後に、キャッシュ済みのアプリ本体を再表示できるservice workerを追加 |
| 公開ワークフロー | `.github/workflows/pages.yml` は手動実行だけ。アプリ用ファイルだけを配信し、文書・レビュー・状態ファイルは含めない |
| 公開状態 | GitHub PagesをGitHub Actionsから有効化済み。ワークフロー#1がSuccess |
| 公開URL | `https://kkrod899.github.io/kajitori-game/`。リポジトリとPagesサイトは公開範囲 |
| データ | 現行v0.2の保存キー `kajitori_stable_mvp_v2` は維持。以前の `file://` 保存データはURL版へ自動移行しない |

公開状態、スマホでの開き方、保存データの境界は `docs/MOBILE_ACCESS.md` に記載した。非公開サイトが必要になった場合は、GitHub Pagesをその用途に使わず、認証付きホスティングを別途選ぶ。

## 未決定事項

| 論点 | 状態 |
|---|---|
| プロダクト名 | D-019 Proposed。現行名維持 / 「見えない半分」系など。v0.2核検証後に判断 |
| 対象期間 | 新生児期中心 / 0〜1歳 / 復職後まで |
| 初期提供形態 | ローカルWebアプリ / PWA / iOS・Android |
| 家族共有 | 初期はなし / 同一端末 / アカウント同期 |
| ゲーム成長表現 | 能力・担当領域のバッジ / マップ / 非数値レベル等。旧XPへの単純回帰はしない |

これらはSlice 01の7〜14日検証を始めるブロッカーではない。
