# v0.3 UX Slice — 実機フィードバック対応

Date: 2026-09-05
Status: Implemented / automated Chromium + WebKit validation Pass / physical iPhone acceptance pending

## 1. 背景

v0.2をiPhoneで実利用した直後、7〜14日テストを続ける前に次の問題が確認された。

- 下部ナビが本文へ重なり、スクロールが不安定になる
- `今やる`を経由するため、通常項目でも完了までのタップが多い
- `状態事実`、`今日の枠`など内部仕様寄りの文言が直感的でない
- カードが縦に長く、一覧性が低い
- 閲覧のみの`担当`タブを固定ナビへ置く価値が低い
- 朝に夜用・明日用の候補が上位へ出るなど、時間帯と優先順位が合わない
- 配色と視覚表現が主要利用者本人の嗜好と合わなかった

この状態ではC-01〜C-05そのものよりUIの摩擦を測ることになるため、実生活テストを一度止め、P1相当の操作・レイアウト問題を先に直す。

## 2. v0.3の目的

プロダクト思想と保存形式を変えず、日常の割り込み下で「見て、その場で処理する」ためのモバイルUXへ刷新する。

## 3. 固定するUI

| 領域 | v0.3 |
|---|---|
| 配色 | 白〜暖色寄りの薄灰を基調に、アクセントはブルーグレー `#5F86A4` |
| イラスト | 使用しない |
| ナビ | `今日 / この先 / 記録` の3タブ |
| 設定 | 右上の歯車 |
| 今日の量 | `0 / 1 / 2 / 3件`を直接選択 |
| 今日一覧 | 最大5件のコンパクトカード。カテゴリ、タイトル、短い補足、目安時間 |
| カテゴリ | `育児 / 家事 / その他` |
| 通常項目 | 一覧のチェックで1タップ完了。詳細からも完了可能 |
| 状態確認項目 | カード→ボトムシート→3段階状態→完了 |
| 詳細 | 下から出るボトムシート |
| 判断理由 | 詳細内の`判断のヒントを見る`へ格納 |
| 低頻度操作 | `…`に`あとで見る / 今日は出さない / もう済んでいる`を格納 |
| 完了取り消し | 完了チェック再タップまたはUndo |
| スクロール | 本文だけをスクロール。下ナビは別レイアウト行とし本文へ重ねない |
| 時間帯 | 朝・昼・夕方以降で候補の並び順を静的に調整 |
| 状態の扱い | 一覧は行動名を見出しにし、状態確認が必要な項目は詳細の最初の実質操作で現在状態を確認する |

## 4. 変えないもの

- 保存キー `kajitori_stable_mvp_v2`
- `v02` namespace / version 2
- 旧rootの `tasksByDate / missedLog / retryQueue`
- 3段階状態の内部値 `enough / soon / out`
- 状態事実の当日鮮度
- パートナー承認、夫婦比較、XP、連続日数を使わない方針
- 証拠イベントは観測できた事実だけを残す境界

v0.3はデータ移行ではなくUX/UI刷新として扱う。

## 5. 今日の件数の意味

`今日の目標 0〜3件`は達成率や評価スコアではなく、その日にどれくらい持つかを示す利用者自身の負荷設定とする。

- `あと1件`などの追い込み表示はしない
- 達成率ゲージは出さない
- 未達を失敗扱いしない
- 候補一覧は最大5件まで判断材料として見せられる
- 目標0件を選んだ日は候補一覧を閉じる

## 6. v0.3で更新した旧仕様

- D-018の独立した「1日1問」カードはTodayから外し、状態確認を該当タスクの詳細へ統合する。既存のquestionHistory等は保持する。
- D-022の最大3件は、v0.3では候補一覧の最大件数ではなく「今日どれくらい背負うか」の負荷設定として扱う。候補一覧は最大5件まで表示できる。
- D-016の目的である「状態から判断する」は維持しつつ、一覧の大見出しはスキャンしやすい行動名にする。状態管理項目は詳細内で現在状態を先に確認する。

詳細はD-028〜D-030を参照。

## 7. 受入条件

### 操作

- [x] 通常項目は一覧から1タップで完了できる
- [x] 状態確認項目は詳細内で状態記録→完了できる
- [x] 完了は取り消せる
- [x] 完了取り消し時に派生証拠も整合する
- [x] `あとで見る`は同日中に自動復帰しない
- [x] `今日は出さない`は同日中にToday/Forecastへ再表示しない
- [x] `この先`から明示追加した項目はTodayで見える

### モバイル

- [x] Chromium 390×844で下部ナビと本文領域が重ならない
- [x] WebKit 390×844で下部ナビと本文領域が重ならない
- [x] ボトムシートの中心操作をChromium/WebKitで実行できる
- [ ] 物理iPhone Safariで下部ナビが本文を隠さない
- [ ] ホーム画面追加版でも本文だけがスクロールできる
- [ ] 物理iPhoneでsafe-areaとservice worker更新を確認する

### データ

- [x] 既存 `kajitori_stable_mvp_v2.v02` を読み込める
- [x] v0.3起動だけで既存記録を削除しない
- [x] 旧root `tasksByDate / missedLog / retryQueue` を保持する
- [x] 既存questionHistoryを保持する
- [x] localStorage書込失敗時のロールバック機構を維持する
- [x] 在庫証拠は観測可能な事実だけを表示する

### 配信

- [x] service workerのcache名を `kajitori-shell-v3` へ更新
- [x] Pages artifactへv0.3 CSS/JSを含める
- [x] `index.html` / manifest / appのtheme colorをブルーグレーで統一する
- [ ] mainへ反映後、Pages workflowを手動実行する
- [ ] 公開URLでv0.3配信を確認する

## 8. 自動検証

GitHub Actions `Validate v0.3 mobile UX` で以下を実行する。

- `node --check`によるJavaScript構文検証
- 必須資産とPages配信対象の参照確認
- Playwright Chromium 390×844
- Playwright WebKit 390×844
- 0件/3件設定
- 状態項目の記録・完了・証拠生成
- 完了取り消しと証拠削除・再生成
- 通常項目の1タップ完了
- Forecast→Today明示追加
- 3タブ遷移
- 旧保存データ保持
- console/page error 0
- スクリーンショット保存

Run #9 `33934349296` は Chromium / WebKitともPass。事後監査は `reviews/2026-09-05-0005-chatgpt-v0-3-ux-post.md` に記録した。

## 9. 実装ファイル

- `kajitori_minimal_pictogram_compact.html`
- `kajitori_v03.css`
- `kajitori_v03_core.js`
- `kajitori_v03_actions.js`
- `kajitori_v03_ui.js`
- `index.html`
- `manifest.webmanifest`
- `sw.js`
- `.github/workflows/pages.yml`
- `tests/v03_smoke.mjs`
- `.github/workflows/v03-validate.yml`

## 10. 公開前の停止条件

以下のいずれかが残る場合はPages本番公開を止める。

- JavaScript構文エラー
- 既存保存データを壊す変更
- 自動WebKit検証で下部ナビの重なりが再発
- 完了・Undo・状態記録の中心フローが自動検証で動かない
- P0/P1 findingが未解決

自動検証ではP0/P1なし。物理iPhone Safari / ホーム画面版はPages反映後の最終受入ゲートとして残す。
